<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asha.dshm.asha2">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login.LoginActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".Form1" />
        <activity android:name=".SubActivityChoice" />
        <activity android:name=".ActivityChoice"></activity>
        <activity android:name=".Form3" />
        <activity android:name=".Form2"></activity>
        <activity android:name=".Form4" />
        <activity android:name=".Family_Details" />
        <activity android:name=".Form5"></activity>
        <activity android:name=".Form6"></activity>
        <activity android:name=".treatment_details_form5" />
        <activity android:name=".FillChoice"></activity>
        <activity android:name=".Resurvey" />
        <activity android:name=".rntcp" />
        <activity android:name=".ChangeLanguage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>